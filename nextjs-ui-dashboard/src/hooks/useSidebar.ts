/**
 * useSidebar Hook
 *
 * Re-exports from SidebarContext for backwards compatibility.
 * The sidebar state is now shared via Context instead of local state.
 */

export { useSidebar } from '@/contexts/SidebarContext';
