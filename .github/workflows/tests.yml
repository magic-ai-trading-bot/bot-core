name: Test Suite Overview

# This is now a meta-workflow that summarizes individual service tests
# Individual services have their own dedicated workflows:
# - python-tests.yml: Python AI Service Tests
# - rust-tests.yml: Rust Core Engine Tests  
# - nextjs-tests.yml: Next.js Dashboard Tests
# - integration-tests.yml: Cross-service Integration Tests
# - security-scan.yml: Security Scanning

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test-overview:
    runs-on: ubuntu-latest
    name: Test Suite Overview
    
    steps:
    - name: Test Suite Information
      run: |
        echo "🧪 Bot Core Test Suite"
        echo "====================="
        echo ""
        echo "Individual test workflows:"
        echo "• Python AI Service Tests: .github/workflows/python-tests.yml"
        echo "• Rust Core Engine Tests: .github/workflows/rust-tests.yml" 
        echo "• Next.js Dashboard Tests: .github/workflows/nextjs-tests.yml"
        echo "• Integration Tests: .github/workflows/integration-tests.yml"
        echo "• Security Scans: .github/workflows/security-scan.yml"
        echo ""
        echo "Coverage target: >90% for each service"
        echo ""
        echo "Local testing:"
        echo "• make test          # Run all tests"
        echo "• make test-python   # Python tests only"
        echo "• make test-rust     # Rust tests only"
        echo "• make test-frontend # Next.js tests only"