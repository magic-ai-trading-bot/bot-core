{
  "name": "health-check",
  "schedule": "0 */2 * * *",
  "timeout_seconds": 180,
  "no_deliver": true,
  "prompt": "Run `botcore check_system_health`. If ALL services are healthy, do nothing — just say OK. If ANY service is unhealthy or erroring, immediately run `botcore send_telegram_notification` with an URGENT alert including which services are down and error details."
}
