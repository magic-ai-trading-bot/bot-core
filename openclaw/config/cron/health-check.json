{
  "name": "health-check",
  "schedule": "0 */2 * * *",
  "timeout_seconds": 180,
  "no_deliver": true,
  "prompt": "Quick system health check. Run `botcore check_system_health`. If ALL services are healthy, respond with exactly ALL_HEALTHY and nothing else - do NOT send any notification. ONLY if something is wrong: send an URGENT alert to Telegram using `botcore send_telegram_notification` with the alert details. Be specific and urgent in alerts."
}
