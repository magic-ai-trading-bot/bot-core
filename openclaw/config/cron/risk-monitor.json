{
  "name": "risk-monitor",
  "schedule": "*/30 * * * *",
  "timeout_seconds": 180,
  "no_deliver": true,
  "prompt": "Run `botcore get_paper_portfolio` and `botcore get_paper_trading_status`. If daily loss exceeds 3%, paper trading stopped unexpectedly, or any critical issue: immediately run `botcore send_telegram_notification` with URGENT alert details. If everything is normal, do nothing — just say OK."
}
